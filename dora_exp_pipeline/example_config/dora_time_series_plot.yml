# Data loading module parameters
data_type: 'Time series'
data_to_fit: 'sample_data/earth_fieldsamples/sentinel1_ndvi_fit.csv'
data_to_score: 'sample_data/earth_fieldsamples/sentinel1_ndvi_predict.csv'
zscore_normalization: False
'out_dir': 'results'

# Feature extraction module
features: {
    raw_values: {
        # no args
    }
}

# Outlier detection module
# Outlier detection module
outlier_detection: {
    pca: {
        k: 3
    },
    iforest: {}
}

# Results organization module
results: {
    comparison_plot: {
        'validation_dir': 'sample_data/earth_fieldsamples/sentinel1_ndvi_labels.csv'
    }
}